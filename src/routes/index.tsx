import { Url, Mode as ModeType } from "routes/paths";
import { getBreads } from "routes/breadcrumbs";
import Routes from "./Routes";

export { Routes, Url, getBreads };
export type Mode = ModeType;
export default Routes;